# Blog

A zero-build static blog. Markdown files in `content/` are rendered client-side with [marked](https://github.com/markedjs/marked) and routed via URL hash (`#/id`). Posts are discovered automatically via binary search - just add the next numbered `.md` file.
